import { SortByCategoryNamePipe } from './sort-by-category-name.pipe';

describe('SortByCategoryNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SortByCategoryNamePipe();
    expect(pipe).toBeTruthy();
  });
});
