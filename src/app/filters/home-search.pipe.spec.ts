import { HomeSearchPipe } from './home-search.pipe';

describe('HomeSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new HomeSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
