import { DistanceMinMaxRangePipe } from './distance-min-max-range.pipe';

describe('DistanceMinMaxRangePipe', () => {
  it('create an instance', () => {
    const pipe = new DistanceMinMaxRangePipe();
    expect(pipe).toBeTruthy();
  });
});
