import { SortCityNamePipe } from './sort-city-name.pipe';

describe('SortCityNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SortCityNamePipe();
    expect(pipe).toBeTruthy();
  });
});
